# 🤖 Agent Marketplace

Платформа для покупки и продажи AI-агентов.

## Возможности

- 📦 Загрузка агентов (ZIP архив с кодом + промпты)
- 💳 Оплата через Oxapay (USDT)
- 💰 Вывод средств продавцам (70% - нам 30%)
- 👤 Личные кабинеты покупателя и продавца
- 📊 Статистика продаж

## Быстрый старт

### 1. Клонирование

```bash
git clone https://github.com/your-repo/agent-marketplace.git
cd agent-marketplace
```

### 2. Настройка

```bash
cp .env.example .env
# Отредактируй .env
```

### 3. Деплой

```bash
chmod +x deploy.sh
./deploy.sh your-domain.com
```

## Структура проекта

```
├── app/                    # Next.js приложение
│   ├── api/              # API routes
│   ├── page.tsx          # Главная страница
│   └── layout.tsx        # Макет
├── prisma/
│   └── schema.prisma     # База данных
├── lib/
│   ├── prisma.ts        # DB клиент
│   └── oxapay.ts       # Oxapay интеграция
├── docker-compose.yml    # Docker Compose
├── nginx.conf           # Nginx конфиг
└── deploy.sh            # Деплой скрипт
```

## API Endpoints

### Agents
- `GET /api/agents` - Список агентов
- `GET /api/agents/[id]` - Агент
- `POST /api/agents/create` - Создать агента

### Purchases
- `POST /api/purchases` - Создать платёж

### Webhooks
- `POST /api/webhooks/oxapay` - Callback от Oxapay

## Комиссии

| Роль | Процент |
|------|---------|
| Покупатель | 0% |
| Продавец | 70% |
| Платформа | 30% |

## Лицензия

MIT
